/*
const request = require('request');
const package = require('../package.json');
const test_routes = require('./test_routes.js');
/*
describe('run', () => {

  const app = require('../app.js').run(package, {'/': test_routes});

  describe('GET', () => {
    it('should GET routes', (done) => {
      request
    });
  });
});
*/